import { FilteredForOfDirective } from './filtered-for-of.directive';

describe('FilteredForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new FilteredForOfDirective();
    expect(directive).toBeTruthy();
  });
});
